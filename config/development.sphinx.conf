indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Users/steve/rails/touristr/log/searchd.log
  query_log = /Users/steve/rails/touristr/log/searchd.query.log
  pid_file = /Users/steve/rails/touristr/log/searchd.development.pid
}

source destination_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = touristr_development
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `destinations`.`id` * 1 + 0 AS `id` , CAST(`destinations`.`name` AS CHAR) AS `name`, CAST(`destinations`.`feature_class` AS CHAR) AS `feature_class`, CAST(`destinations`.`feature_code` AS CHAR) AS `feature_code`, CAST(`destinations`.`region_name` AS CHAR) AS `region_name`, CAST(`destinations`.`country_code` AS CHAR) AS `country_code`, CAST(`destinations`.`admin1_code` AS CHAR) AS `admin1_code`, CAST(`destinations`.`admin2_code` AS CHAR) AS `admin2_code`, CAST(`destinations`.`score` AS CHAR) AS `score`, IFNULL(`destinations`.`score`, '') AS `score_sort`, `destinations`.`id` AS `sphinx_internal_id`, 3092951421 AS `class_crc`, '3092951421' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM destinations    WHERE `destinations`.`id` >= $start   AND `destinations`.`id` <= $end    GROUP BY `destinations`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `destinations` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_str2ordinal = score_sort
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `destinations` WHERE  `id` = (($id - 0) / 1)
}

index destination_core
{
  source = destination_core_0
  path = /Users/steve/rails/touristr/db/sphinx/development/destination_core
  morphology = stem_en
  charset_type = utf-8
}

index destination
{
  type = distributed
  local = destination_core
}
